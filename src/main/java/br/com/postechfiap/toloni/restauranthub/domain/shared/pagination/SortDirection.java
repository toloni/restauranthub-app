package br.com.postechfiap.toloni.restauranthub.domain.shared.pagination;

/// Represents the direction of a sort operation.
public enum SortDirection {

    /// Sorts results in ascending order (A → Z, 0 → 9).
    ASC,

    /// Sorts results in descending order (Z → A, 9 → 0).
    DESC
}